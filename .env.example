# ─── Microsoft Entra ID (Azure AD) ───────────────────────────────
# Register an app at https://portal.azure.com → App registrations
# Set redirect URI to: {APP_URL}/api/auth/callback
ENTRA_TENANT_ID=your-tenant-id
ENTRA_CLIENT_ID=your-client-id

# ─── Application ─────────────────────────────────────────────────
APP_URL=http://localhost:4321

# ─── Encryption ──────────────────────────────────────────────────
# Master secret for AES-GCM key derivation (min 32 chars recommended)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY_SECRET=your-encryption-master-secret

# ─── Cloudflare (for drizzle-kit migrations) ─────────────────────
CLOUDFLARE_ACCOUNT_ID=your-account-id
CLOUDFLARE_D1_DATABASE_ID=your-d1-database-id
CLOUDFLARE_API_TOKEN=your-api-token

# ─── Wrangler Secrets (set via CLI, not .env) ────────────────────
# wrangler secret put ENTRA_CLIENT_SECRET
# wrangler secret put ENCRYPTION_KEY_SECRET
